const first = [1, 2, 3, 4];
const joined = first.join("_");

console.log(joined);
const message = "THis is a big message";
const parts = message.split(" ");

console.log(parts);
message_jioned = parts.join("___");

console.log(message_jioned);
